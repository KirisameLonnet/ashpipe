{
  "description": "Auto-mount ashpipe portals when Claude Code starts in an ashpipe workspace",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "if command -v ashpipe >/dev/null 2>&1 && ashpipe status >/dev/null 2>&1; then ashpipe mount 2>&1; fi"
          }
        ]
      }
    ]
  }
}
